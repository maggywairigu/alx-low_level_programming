#include "main.h"
/**
 *_strlen - Returns the value of a string
 *@s: pointer to the string value
 *Description: Returns the length of the value of *s
 *Return: length of *s
 */
int _strlen(char *s)
{
	int i,l;

	for (i = 0, s[i] != '\0', i++)
	{
		l = i;
	}
	return[l];
}
