#include "main.h"
/**
 *mul - multiplies two numbers and returns their result
 *@a: first number
 *@b: second number
 *Return: The result of the multiplication of a and b
 */
int mul(int a, int b)
{
	return (a*b);
}
